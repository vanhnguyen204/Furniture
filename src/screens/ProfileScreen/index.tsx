import React from 'react';
import Container from '../../components/Container.tsx';
import TextComponent from '../../components/TextComponent.tsx';

const ProfileScreen = () => {
  return (
    <Container>
      <TextComponent value={'Profile'} />
    </Container>
  );
};

export default ProfileScreen;
